fn seq (start, end)
  start < end ?
    start :: seq (start + 1, end)
    end ;

fn map (f, lst)
  lst ?
    f (lst:h) :: map (f, lst:t)
    lst ;

fn square (n) n * n ;

list = seq (1, 10)
print list
print map (square, list)
